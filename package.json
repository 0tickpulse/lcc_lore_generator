{
  "name": "lcc_lore_generator",
  "version": "1.0.0",
  "description": "Generates lore",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "start": "node dist/index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "yaml": "^2.1.3"
  }
}
